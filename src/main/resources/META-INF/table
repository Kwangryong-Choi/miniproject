create table member_join(
	midx int unsigned auto_increment,	/* �ڵ� ������ */
	email varchar(100) not null,	/* �̸��� */
	password varchar(100) not null,	/* ��й�ȣ */
	user_name varchar(50) not null,	/* ������̸� */
	phone_num varchar(50) not null,	/* ��ȭ��ȣ */
	ad_use enum('Y','N') not null default 'N',		/* ���� ��/�� */
	today timestamp not null default current_timestamp,	/* �������� */
	primary key(midx)
)

drop table member_join

create table md_choice(
	
)